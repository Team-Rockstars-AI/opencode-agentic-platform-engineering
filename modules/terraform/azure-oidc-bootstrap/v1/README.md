# Azure OIDC Bootstrap Module

This module provisions an Azure User-Assigned Managed Identity and configures the **Federated Identity Credentials** required to authenticate GitHub Actions passwordless logins without storing credentials in source files.

## Usage
Refer to standard deployment templates.
